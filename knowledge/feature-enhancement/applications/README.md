# 系统功能增强：应用体验

应用体验增强优先判断是否属于独立项目开发。如果是具体应用的 UI、构建、验证或交互开发要求，应转到对应项目目录的项目级提示词；本知识库只保留可复用的系统集成边界。

如果增强需求实际变成安装失败、桌面入口异常或隔离边界失效，切换到 `$HOME/.os-fix-skill/SKILL.md`，再按 `references/system-repair/applications.md` 路由到对应修复知识。

如果后续出现纯系统集成功能增强需求，在本目录新增独立章节，并从本 README 链接过去；不要把独立应用项目的开发规范写入本 skill。
